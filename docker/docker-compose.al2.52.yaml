version: "3"

services:

  runtime-setup:
    image: swift-aws-lambda-events:al2-5.2
    build:
      args:
        swift_version: "5.2"

  test:
    image: swift-aws-lambda-events:al2-5.2

  test-samples:
    image: swift-aws-lambda-events:al2-5.2

  shell:
    image: swift-aws-lambda-events:al2-5.2
